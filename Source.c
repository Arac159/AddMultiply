#include <stdlib.h>

int suma (int a, int b)
{
	return a+b;
}
int produs (int a, int b)
{
	return a*b;
}

void main()
{
	int a = 0, b = 0;
	scanf_s("%d%d", a, b);
	printf("%d%d", suma(a,b), produs(a,b);
	
}
